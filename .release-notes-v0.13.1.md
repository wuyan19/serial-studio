## 🔌 开发板接入（本版重点）

不跑完整 Serial Studio 也能把开发板串口接进来：`ss-board-bridge` 单文件 C 实现（纯 libc、静态约 65KB）跑在板上，PC 端 UI 注册设备地址后，端口即以 `昵称::端口名` 出现在 Web UI / 脚本 / MCP 工具——AI agent 经 MCP 直达板子串口。

- Release 附带四个架构的预编译静态二进制（x86_64 / aarch64 / armv7 / riscv64）及 sha256 清单，scp 到板子即可，零编译；mips/mipsel 可一条 musl-gcc 命令从源码编译。
- 设备端协议已文档化为公开契约（`docs/device-protocol.md`），任何语言可实现；`examples/ss-board-bridge.c` 为官方参考实现。

## ℹ️ 说明

- 应用本体无功能变化（版本号仅为发布上述资产而递增），桌面用户无需急着升级。
